# Bot-request evidence

Use the repository URL in the request's `Code:` field:

```text
https://github.com/ProjectCatMapper/CatMapperBot
```

The request should link to the committed manifest checksum in `data/sociomap-ethnicity-p14249-manifest.json`, this repository's commit SHA, and the editing policy. No credentials belong in the repository, manifest, issue tracker, or bot request.

If requesting the ArchaMap task, describe it separately as a provisional `P2888` exact-match URL workflow. It uses only `https://catmapper.org/archamap/AM…` URLs from a separately reviewed manifest and must end or be revised when an ArchaMap-specific identifier property is approved.

